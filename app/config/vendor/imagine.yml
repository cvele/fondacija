liip_imagine:
    resolvers:
       default:
          web_path: ~

    filter_sets:
        cache: ~
        avatar:
            quality: 100
            filters:
                thumbnail: { size: [200, 200], mode: outbound }
